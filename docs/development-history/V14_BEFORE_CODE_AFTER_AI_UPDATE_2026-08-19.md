# V14 update — Before code render / After AI only

- BEFORE is now rendered immediately from measured layout coordinates with deterministic code.
- Only AFTER is generated by the image model, reducing image API calls to one per selected improvement set.
- Removed the first AI Before generation and the Before/After paired generation path from the active Flask route.
- Action Plan UI now labels BEFORE as the coordinate-based plan and AFTER as the AI visualization.
- PDF download becomes available after the AFTER AI image is ready.
- PDF includes the coordinate-based BEFORE plan and the generated AFTER AI image.
- AFTER generation uses one image edit request with the measured floorplan as the reference image; no edit->generate network fallback is used in this fast path.
